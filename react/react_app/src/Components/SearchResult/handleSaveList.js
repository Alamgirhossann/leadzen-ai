const handleSaveList = (data) => {
  console.log("e.target", data);
  // selectedSaveList([...selectedSaveList,id]);
  // if(!checked){
  //   selectedSaveList(selectedSaveList.filter((item) => item !== id))
  // }
  // console.log("selectedSaveList",selectedSaveList)
};
export default handleSaveList();
